---
name: Navigation Panel
menu: Components
---

import { Box } from '@theme-ui/components';
import { Playground, Props } from 'docz';
import { NavPanel } from '.';
import { DemoBox } from './demo-box';
import { NavLink } from '../navigation-link';

# Navigation Panel

## Props glossary

A short list of the components properties

## Component

<Playground>
  <DemoBox>
    <NavPanel isExpanded={true}>
      <NavLink href="#" current={false} isFocused={true} context="panel">
        Bikinis
      </NavLink>
      <NavLink href="#" current={true} isFocused={false} context="panel">
        One Pieces
      </NavLink>
      <NavLink href="#" current={false} isFocused={false} context="panel">
        Yoga & Surf
      </NavLink>
      <NavLink href="#" current={false} isFocused={false} context="panel">
        Tanning
      </NavLink>
      <NavLink href="#" current={false} isFocused={false} context="panel">
        Healing Flowers
      </NavLink>
      <NavLink href="#" current={false} isFocused={false} context="panel">
        The Brand
      </NavLink>
      <NavLink href="#" current={false} isFocused={false} context="panel">
        Sustainability
      </NavLink>
      <NavLink href="#" current={false} isFocused={false} context="panel">
        Fabric
      </NavLink>
    </NavPanel>
  </DemoBox>
</Playground>
