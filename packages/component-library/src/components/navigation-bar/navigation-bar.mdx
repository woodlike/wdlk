---
name: Navigation Bar
menu: Components
---

import { Playground, Props } from 'docz';
import { NavBar } from './';
import { NavLink } from '../navigation-link';

# Navigation Bar

## Props glossary

A short list of the components properties
<Props of={NavBar} />

## Component

<Playground>
  <NavBar itemCount={6}>
    <NavLink href="#" current={false} isFocused={true} text="Bikinis" />
    <NavLink href="#" current={true} isFocused={false} text="One Pieces" />
    <NavLink href="#" current={false} isFocused={false} text="Yoga & Surf" />
    <NavLink href="#" current={false} isFocused={false} text="Tanning" />
    <NavLink href="#" current={false} isFocused={false} text="Healing Flowers" />
    <NavLink href="#" current={false} isFocused={false} text="The Brand" />
  </NavBar>
</Playground>
