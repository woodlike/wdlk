---
name: Header
menu: Components
---

import { Playground, Props } from 'docz';

import { Header } from './';
import { Logo } from '../logo';
import { NavBar } from '../navigation-bar';
import { NavLink } from '../navigation-link';

# Header

## Props glossary

A short list of the components properties

<Props of={NavBar} />

## Full example

<Playground>
  <Header
    areas={[
      <Logo title="demo title" desc="desc" isFocused={true} />,
      <NavBar itemCount={6}>
        <NavLink
          href="#"
          current={false}
          isFocused={false}
          text="Bikinis"
          context="bar"
        />
        <NavLink
          href="#"
          current={true}
          isFocused={false}
          text="One Pieces"
          context="bar"
        />
        <NavLink
          href="#"
          current={false}
          isFocused={false}
          text="Yoga & Surf"
          context="bar"
        />
        <NavLink
          href="#"
          current={false}
          isFocused={false}
          text="Tanning"
          context="bar"
        />
        <NavLink
          href="#"
          current={false}
          isFocused={false}
          text="Sustainability"
          context="bar"
        />
        <NavLink
          href="#"
          current={false}
          isFocused={false}
          text="The Brand"
          context="bar"
        />
      </NavBar>,
      <Logo title="demo title" desc="desc" isFocused={false} />,
    ]}
  />
</Playground>
