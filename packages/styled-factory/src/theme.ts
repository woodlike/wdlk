import { colors, fontFamily, fontSizes, fontWeights, spaces } from './tokens';

export const theme = {
  colors,
  fonts: {
    ...fontFamily
  },
  fontSizes,
  fontWeights,
  spaces
};
export default theme;
