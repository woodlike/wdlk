---
name: Cart
menu: Components
---

import { Cart, Code, Language } from '@wdlk/components';

# Cart

## Empty cart

<Code
  code={`<pre sx={createStylesPre(props.size, qt, props.theme)}>
    <code sx={stylesCode}>
      {tokens.map((token: Token | string, i: number): JSX.Element => {
        console.log(tokens);
        return token.type ? (
          Array.isArray(token.content) ? (
            <TokenList content={token.content} theme={theme} />
          ) : (
            <span sx={theme.get(token.type)}>
              {typeof token.content === 'string' && token.content}
            </span>
          )
        ) : (
          <span>{token}</span>
        );
      })}
    </code>
  </pre>`}
  lang={Language.tsx}
  size="l"
/>
<Cart href="#" title="Woodlike Ocean shopping cart" count={0} isFilled={false} />

## Cart with items

<Cart href="#" title="Woodlike Ocean shopping cart" count={1} isFilled={true} />

## Focused Cart

<Cart isFocused={true} href="#" title="Woodlike Ocean shopping cart" count={1} isFilled={true} />
