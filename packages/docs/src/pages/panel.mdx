---
name: Navigation Panel
menu: Components
---

import { Playground, Props } from 'docz';
import * as Nav from '@wdlk/components';
import { DemoBox } from '@wdlk/components';

# Navigation Panel

## Props glossary

A short list of the components properties

## Component

<Playground>
  <DemoBox>
    <Nav.Panel isExpanded={true}>
      <Nav.Link href="#" current={false} isFocused={true} context="panel" text="Bikinis" />
      <Nav.Link href="#" current={true} isFocused={false} context="panel" text="One Pieces" />
      <Nav.Link href="#" current={false} isFocused={false} context="panel" text="Yoga & Surf" />
      <Nav.Link href="#" current={false} isFocused={false} context="panel" text="Tanning" />
      <Nav.Link href="#" current={false} isFocused={false} context="panel" text="Healing Flowers" />
      <Nav.Link href="#" current={false} isFocused={false} context="panel" text="The Brand" />
      <Nav.Link href="#" current={false} isFocused={false} context="panel" text="Sustainability" />
      <Nav.Link href="#" current={false} isFocused={false} context="panel" text="Fabric" />
    </Nav.Panel>
  </DemoBox>
</Playground>
