import { FontKey, FontValue } from '../fonts';

export const fontFamily: Record<FontKey, FontValue> = {
  body: `"museo_sans300", Helvetica, sans-serif`,
  heading: {
    display: `"museo300", serif`,
    secondary: `"museo500", serif`,
  },
};
